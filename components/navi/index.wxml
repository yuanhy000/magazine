<view class="container">
	<image bind:tap="onLeft" class="icon" src="{{latest ? disLeftSrc : leftSrc}}" mode="scaleToFill"></image>
	<text class="title">{{title}}</text>
	<image bind:tap="onRight" class="icon" src="{{first ? disRightSrc : rightSrc}}" mode="scaleToFill"></image>
</view>